#include "Mesh.h"

mesh::Mesh::Mesh():size(0)
{
}

mesh::Mesh::Mesh(unsigned int size):
	size(size)/*,
	m_Position(size),m_Normal(size),m_Tangent(size),m_Color(size),
	m_Texcoord0(size), m_Texcoord1(size), m_Texcoord2(size), m_Texcoord3(size)*/
{
}
